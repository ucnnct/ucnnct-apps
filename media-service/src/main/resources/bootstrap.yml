spring:
  application:
    name: media-service
  cloud:
    vault:
      host: ${VAULT_HOST:172.31.253.76}
      port: ${VAULT_PORT:8200}
      scheme: https
      authentication: KUBERNETES
      kubernetes:
        role: ucnnct
        service-account-token-file: /var/run/secrets/kubernetes.io/serviceaccount/token
      kv:
        version: 2
      ssl:
        verify-hostname: false
        skip-ssl-verify: true

