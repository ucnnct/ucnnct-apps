spring:
  application:
    name: chat-service
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/chatdb}
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: chat-service
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: ${KEYCLOAK_JWKSET_URI:http://localhost:8882/realms/ucnnct/protocol/openid-connect/certs}
          issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8882/realms/ucnnct}

server:
  port: 8084

app:
  kafka:
    topics:
      chat-messages: ${WS_KAFKA_TOPIC_CHAT_MESSAGES:message.send}
      messages-persisted: ${WS_KAFKA_TOPIC_MESSAGES:message.persisted}
      message-status-updates: ${WS_KAFKA_TOPIC_MESSAGE_STATUS_UPDATES:message.status.update}
      message-read-legacy: ${CHAT_LEGACY_MESSAGE_READ_TOPIC:message.read}
      message-read-confirmed: ${CHAT_MESSAGE_READ_CONFIRMED_TOPIC:message.read.confirmed}
